public static int getLeafSize(Node root) {
        if (root == null) {
            return 0;
        }
        if (root.left == null && root.right == null) {
            return 1;
        }
        //求root的叶子结点，左子树叶子结点+右.....
        return getLeafSize(root.left) + getLeafSize(root.right);
}
